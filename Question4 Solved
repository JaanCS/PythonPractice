#Create a program that asks the user for a number and then prints out a list of all the divisors of that number.

num = input("Give me a number and I'll present you with its divisors: ")
x=range(1,(int(num)+1))
d=[elem for elem in x if int(num)%elem ==0]
print(d)
