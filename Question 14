# Write a program that takes a list
# and returns a new list that
# contains all the elements of the first list
# minus all the duplicates
# Write two different functions to do this
# one using a loop and constructing a list
# another using sets


def dloop(list):
    dlist = []
    for element in list:
        if element in dlist:
            dlist.remove(element)
        dlist.append(element)
    return dlist



def dset(klist):
    d_set = list(set(klist))
    return d_set


